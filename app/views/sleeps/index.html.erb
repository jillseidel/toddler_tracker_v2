<div class="page-header">
  <% child = Child.find_by(:id => cookies[:child_id]) %>
  <h1>
    <%= child.name %>'s Sleep
  </h1>
    <a href="/sleeps/new" class="btn btn-lg btn-success">New Sleep Entry</a>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Date</th>
        <th>Naps</th>
        <th>Wake ups</th>
        <th>Day sleep</th>
        <th>Night sleep</th>
        <th>Actions</th>
      </tr>

      <% @sleeps.each do |sleep| %>
      <tr>
        <td><%= sleep.date.strftime('%b %e, %Y') %></td>
        <td><%= sleep.naps %></td>
        <td><%= sleep.wake_ups %></td>
        <td><%= sleep.day_sleep %></td>
        <td><%= sleep.night_sleep %></td>
        <td>
          <a href="/sleeps/<%= sleep.id %>" class="btn btn-primary">Show</a>
          <a href="/sleeps/<%= sleep.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_sleep/<%= sleep.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
